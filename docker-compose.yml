version: '3'

services:
  app:
    image: dielito2010/webacademy1:latest
    container_name: webacademydemo
    restart: always
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production